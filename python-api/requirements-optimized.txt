# Optimized requirements for Railway deployment (<4GB)
# CPU-only PyTorch to save ~2.7GB

# Core API
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
openai==1.12.0
Pillow==10.2.0
numpy==1.26.3
pydantic==2.5.3

# NLP Libraries
spacy==3.7.2
nltk==3.8.1
scikit-learn==1.3.2
underthesea==6.7.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0

# Embedding & Semantic Search (CPU-only)
sentence-transformers>=2.2.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.9.0

# BM25 for lexical filtering
rank-bm25==0.2.2

# IPA Phonetics for flashcards
eng-to-ipa>=0.0.2

# REMOVED to save space:
# ultralytics==8.1.0      # -500MB (YOLO not needed)
# easyocr==1.7.1          # -500MB (OCR not needed)
# matplotlib==3.8.2       # -200MB (Visualization not needed in production)
