# ULTRA-MINIMAL requirements for Railway deployment
# Strategy: NO heavy ML libraries, use API-based embeddings instead

# Core API (fast install)
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
pydantic==2.5.3

# Basic utilities (pre-built wheels available)
numpy==1.26.3

# Document processing (lightweight)
PyPDF2==3.0.1
python-docx==1.1.0

# NLP - Minimal set (NO spacy, NO torch)
nltk==3.8.1
scikit-learn==1.3.2
rank-bm25==0.2.2

# REMOVED to avoid timeout:
# - spacy (requires compilation, 200MB+, 5+ minutes)
# - sentence-transformers (requires torch, 2GB+, 10+ minutes)
# - torch (2GB+, causes timeout)
# - transformers (500MB+, requires torch)
# - Pillow (not critical for text processing)

# SOLUTION: Use OpenAI API for embeddings (no local install)
# Or pre-compute embeddings offline and load from file

# Total install time: ~2 minutes (well under 10 minute timeout)
