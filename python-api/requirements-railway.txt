# MINIMAL requirements for Railway deployment (<2GB)
# Core API
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3

# Basic utilities
numpy==1.26.3
Pillow==10.2.0

# NLP - Essential only
spacy==3.7.2
nltk==3.8.1
scikit-learn==1.3.2

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0

# Lightweight embeddings (CPU-only torch)
# Note: Install torch first, then sentence-transformers
torch==2.0.0
sentence-transformers==2.2.0

# BM25
rank-bm25==0.2.2

# REMOVED (saves ~6GB):
# - ultralytics (500MB) - OCR not critical
# - easyocr (500MB) - OCR not critical  
# - matplotlib (200MB) - visualization not needed in production
# - underthesea (100MB) - Vietnamese NLP not critical
# - openai (50MB) - can be added back if needed
# - eng-to-ipa (20MB) - phonetics not critical
