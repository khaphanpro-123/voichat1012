# ULTRA-MINIMAL requirements for Railway (<2GB target)
# Only absolute essentials for vocabulary extraction API

# Core API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Basic utilities
numpy==1.26.3
pydantic==2.5.3

# NLP - Minimal set
spacy==3.7.2
nltk==3.8.1
scikit-learn==1.3.2

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0

# Lightweight embeddings (NO PyTorch!)
# Use spacy's built-in vectors instead of sentence-transformers

# BM25 for filtering
rank-bm25==0.2.2

# REMOVED (saves ~6GB):
# - torch/torchvision (3GB)
# - sentence-transformers (1GB) 
# - ultralytics (500MB)
# - easyocr (500MB)
# - matplotlib (200MB)
# - underthesea (100MB) - Vietnamese NLP not critical
# - openai (50MB) - Can be added back if needed
# - eng-to-ipa (20MB) - Phonetics not critical

# Total size: ~1.5-2GB
